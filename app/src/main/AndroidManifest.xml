<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.romi.futsal">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />
    <uses-permission-sdk-23 android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Futsal"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".LoginActivity" android:theme="@style/AppTheme.noActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MapsActivity"
            android:label="Lokasi" />
        <activity android:name=".RegisterActivity" android:theme="@style/AppTheme.noActionBar"/>
        <activity android:name=".IndexActivity" android:label="Dashboard"/>
        <activity android:name=".PeraturanActivity" android:theme="@style/AppTheme.noActionBar"/>
        <activity android:name=".TipsActivity" android:theme="@style/AppTheme.noActionBar"/>
        <activity android:name=".FasilitasActivity" android:theme="@style/AppTheme.noActionBar"/>
        <activity android:name=".PemesananActivity" android:theme="@style/AppTheme.noActionBar"/>
        <activity android:name=".ListActivity" android:theme="@style/AppTheme.noActionBar"/>
        <activity android:name=".DetailActivity" android:theme="@style/AppTheme.noActionBar"/>
        <activity android:name=".PengingatActivity" android:theme="@style/AppTheme.noActionBar"/>
        <receiver
            android:name=".MyAlarm"
            android:enabled="true"
            android:exported="true"/>
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
        <uses-library android:name="com.google.android.maps" />
    </application>

</manifest>